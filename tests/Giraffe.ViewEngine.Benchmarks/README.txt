When running the benchmarks with .NET Core >= 3.0, running Giraffe.ViewEngine.Benchmarks.exe will throw exceptions.

Instead, call it via the .NET CLI, e.g.: `dotnet run Giraffe.ViewEngine.Benchmarks.dll`
