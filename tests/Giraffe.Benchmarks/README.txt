When running the benchmarks with .NetCore >= 3.0, running Giraffe.Benchmarks.exe results in exceptions.

Instead, call it the same way as a .NetCore ~2 application (i.e. dotnet run Giraffe.Benchmarks.dll)
